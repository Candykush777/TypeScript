"use strict";
let arregloNumeros = [1, 2, 3, 4, 5, 6]; //no pondriamos any ya que es mejor personalizar tipados para arrays
let arregloTexto = ['HTML', 'CSS', 'JS'];
arregloTexto[0].indexOf('HTML');
console.log(arregloTexto);
